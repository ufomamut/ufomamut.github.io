# Conway Life Simulation – Portfolio Homepage

An interactive visualization of **Conway’s Game of Life** used as the landing page for my personal developer portfolio.  
The project was created primarily as a **learning exercise** to practice working with HTML canvas, basic algorithms, and state-based UI logic in plain JavaScript.

[🇨🇿 verze níže](#česky)

---

## About

This project serves as my portfolio entry page and visual introduction.  
The background simulation is based on Conway’s Game of Life and runs continuously on an HTML `<canvas>` element.

The main goal of the project was to:
- learn how to work with canvas rendering
- understand and implement a simple cellular automaton algorithm
- experiment with application state (UI modes, overlays, controls) without using any frameworks

The simulation can be interacted with using control buttons that seed the grid with different levels of randomness.

---

## Features

- Conway’s Game of Life simulation rendered on canvas
- Interactive controls for seeding the initial state
- State-based UI (intro view, projects view, modal)
- Responsive layout for desktop and mobile devices
- Runs as a static site (no backend)

---

## Technologies

- HTML5
- CSS3 (responsive layout, grid, flexbox)
- Vanilla JavaScript
- Canvas API

---

## Usage

Open the project in a modern web browser.  
The simulation starts automatically and serves as a visual background for the portfolio.

The project is deployed as a static site (e.g. GitHub Pages).

---

## Česky

Interaktivní vizualizace **Conwayovy hry života**, která slouží jako vstupní stránka mého programátorského portfolia.  
Projekt vznikl primárně jako **učební projekt** se zaměřením na práci s HTML canvasem, základní algoritmy a stavové chování uživatelského rozhraní v čistém JavaScriptu.

Cílem projektu bylo:
- naučit se pracovat s vykreslováním pomocí `<canvas>`
- pochopit principy buněčného automatu (Conwayova hra života)
- vyzkoušet si řízení stavů aplikace (různé pohledy, překryvy, ovládání) bez použití frameworků

Simulaci je možné ovládat pomocí tlačítek, která umožňují zasít počáteční stav s různou mírou nahodilosti.

Projekt funguje jako statická webová stránka a slouží zároveň jako vizuální i technická ukázka mé práce.
